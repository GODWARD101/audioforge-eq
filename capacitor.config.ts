import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.audioforge.eq",
  appName: "AudioForge EQ",
  webDir: "src/frontend/dist",
};

export default config;
